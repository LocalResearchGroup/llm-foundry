variables:
  data_local: datasets/tmp # datasets/c4_small
  data_remote: hf://datasets/LocalResearchGroup/split-avelina-python-edu/tokenized/avelinapythonedu/full/
  tokenizer_name: HuggingFaceTB/SmolLM2-135M
  global_seed: 42
  max_seq_len: 8192
  run_name: custom_smollm2_135m_training
  device_microbatch_size: 2
  global_train_batch_size: 4

max_seq_len: ${variables.max_seq_len}
run_name: ${variables.run_name}

model:
  name: custom-smollm2-135m
  model_type: smollm2-135m
  pretrained: false
  pretrained_model_name_or_path: HuggingFaceTB/SmolLM2-135M
  use_liger_rms_norm: true
  use_liger_fused_crossentropy: true
  use_liger_mlp: true

tokenizer:
  name: ${variables.tokenizer_name}
  kwargs:
    model_max_length: ${variables.max_seq_len}
  
loggers:
  wandb:
    project: 'liger-kernel'
    entity: 'local-research-group'
    name: 'liger-all-compile(dynamic_f)_bs-2'

# Data loaders
train_loader:
  name: text
  dataset:
    local: ${variables.data_local}
    remote: ${variables.data_remote}
    split: train
    shuffle: true
    max_seq_len: ${variables.max_seq_len}
    shuffle_seed: ${variables.global_seed}
    deorder_only_format: true
    eos_token_id: 0
    # Sequence packing configuration
    sequence_packing: true
    micro_batch_size: ${variables.device_microbatch_size}  # Should match device_train_microbatch_size
    packing_buffer_size: 512  # 5 * device_batch_size
    packing_prefetch_factor: 512
    # Optional: batch size warmup
    # batch_size_warmup_min_size: 2
    # batch_size_warmup_tokens: 1000ba
  drop_last: true
  num_workers: 4

eval_loader:
  name: text
  dataset:
    local: ${variables.data_local}
    remote: ${variables.data_remote}
    split: test
    shuffle: false
    max_seq_len: ${variables.max_seq_len}
    shuffle_seed: ${variables.global_seed}
    deorder_only_format: true
    eos_token_id: 0
    # Sequence packing configuration
    sequence_packing: true
    micro_batch_size: ${variables.device_microbatch_size}  # Should match device_train_microbatch_size
    packing_buffer_size: 512  # 5 * device_batch_size
    packing_prefetch_factor: 512
    # Optional: batch size warmup
    # batch_size_warmup_min_size: 2
    # batch_size_warmup_tokens: 1000ba
  drop_last: false
  num_workers: 4

# Training configuration
scheduler:
  name: cosine_with_warmup
  t_warmup: 100ba # 8192tok 
  alpha_f: 0.1

optimizer:
  name: decoupled_adamw
  lr: 6.0e-4
  betas:
  - 0.9
  - 0.95
  eps: 1.0e-08
  weight_decay: 0.0

algorithms:
  gradient_clipping:
    clipping_type: norm
    clipping_threshold: 1.0

# Training duration and evaluation
max_duration: 100ba # 16384tok 
eval_interval: 100ba # 16384tok 
eval_first: false
eval_subset_num_batches: 10
save_overwrite: true
save_interval: 0ba  # Disable checkpointing

# System configuration
seed: ${variables.global_seed}
device_eval_batch_size: 4
device_train_microbatch_size: ${variables.device_microbatch_size}
global_train_batch_size: ${variables.global_train_batch_size}
precision: amp_bf16

# FSDP configuration
fsdp_config:
  sharding_strategy: FULL_SHARD
  mixed_precision: PURE
  activation_checkpointing: false
  activation_checkpointing_reentrant: false
  activation_cpu_offload: false
  limit_all_gathers: true

# Torch compile configuration
compile_config:
  dynamic: false  # Disable dynamic shapes (required for Triton kernels with liger_kernel)
  # options:
  #   epilogue_fusion: true  # Fuses pointwise ops into templates (requires max-autotune)
  #   max_autotune: true  # Enables automatic tuning of fusion patterns
  #   coordinate_descent_tuning: true  # Enables coordinate descent tuning for optimization

# Logging
progress_bar: true
log_to_console: true
console_log_interval: 1ba

callbacks:
  speed_monitor:
    window_size: 10
  lr_monitor: {}
  memory_monitor: {}
  runtime_estimator: {}
  # batch_inspection:
  #   log_frequency: 1
  #   sample_size: 1
  #   log_to_console: true
  #   log_to_wandb: false
  packing_efficiency:
    log_interval: 100
  # text_generation:
  #   prompts:
  #     - "The future of artificial intelligence is"
  #     - "In a world where technology"
  #     - "def main():"
  #     - "Gravity is"
  #     - "<|im_start|>system\nYou are a helpful AI assistant named SmolLM, trained by Local Research Group<|im_end|>\n<|im_start|>user\nWhat is 1+1?<|im_end|>\n<|im_start|>assistant\n"
  #   max_new_tokens: 50
  #   temperature: 0.7
  #   log_to_wandb: true